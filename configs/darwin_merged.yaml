# Merged population configuration
# Medium mutation rate

program_size: 32
mutation_rate: 0.0005     # Medium mutation
max_iterations: 8192
eval_interval: 10
visualization_interval: 100
random_seed: 42

# Grid settings (full merged grid)
grid_width: 120
grid_height: 45
epochs: 1000
