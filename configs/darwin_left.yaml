# Left population configuration
# Higher mutation rate for more diversity

program_size: 32
mutation_rate: 0.001      # Higher mutation
max_iterations: 8192
eval_interval: 10
visualization_interval: 100
random_seed: 42           # Left population seed

# Grid settings (overridden by Darwin config)
grid_width: 60
grid_height: 45
epochs: 500
